{% macro Checkbox (ID, Name, Value, Label)%}
    <div class="CheckboxContainer">
        <label class="Checkbox">
            <input type="checkbox" id="{{ ID }}" name="{{ Name }}" value="{{ Value }}">
            <span class="checkmark"></span>
        </label>
        <label for="{{ ID }}" class="-sizeS regular">{{Label}}</label>
    </div>
{% endmacro %}